{
  "name": "crypto-project-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "contracts",
    "services/*"
  ],
  "scripts": {
    "start:client": "npm --prefix client start",
    "start:server": "npm --prefix server start",
    "test": "npm --prefix client test && npm --prefix server test"
  },
  "dependencies": {},
  "devDependencies": {}
}
